#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - abc
 *
 * Return: 0 always runs
 */

int main(void)
{
	char m = 48;
	char i = 97;

	while (m < 58)
		{
	putchar (m);
	m++;
		}
	while (i < 103)
		{
	putchar (i);
	i++;
		}
	putchar ('\n');

	return (0);
}
