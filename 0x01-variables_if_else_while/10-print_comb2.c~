#include <stdio.h>

/**
 * main - num, combi
 *
 * Return: 0 always runs
 */

int main(void)
{
	int i;
	int m = 0;

	while (m <= '99')
	{
	i = m / 10;
	putchar (i);
	i = m % 10;
	putchar (i);
		if (m != '99')
	{
	putchar (',');
	putchar (' ');
	}
	else
       {
	putchar ('\n');
	}
	m++;
		}
	return (0);
}
