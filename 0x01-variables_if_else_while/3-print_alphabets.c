#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - abc
 *
 * Return: 0 always runs
 */

int main(void)
{
	char m = 97;
	char i = 65;

	while (m < 123)
		{
		putchar (m);
		m++;
		}
		while (i < 91)
		{
		putchar (i);
		i++;
		}
	putchar ('\n');

return (0);
}
