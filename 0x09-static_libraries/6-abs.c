#include "holberton.h"

/**
 * _abs - value absolute
 * @a: input int
 *
* Description: statement to if
*
* Return: absolute value
*/

int _abs(int a)
{
	if (a < 0)
	{
	return (a * -1);
	}
	else
	{
	return (a);
	}
}
