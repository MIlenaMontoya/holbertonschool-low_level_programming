#include <stdio.h>

/**
 * main - argc argv
 * @argc: int
 * @argv: char
 *
 * Return: 0
 * On error, -1 is returned, and errno is set appropriately.
 */

int main(int argc, char *argv[])
{
	if (argc)

	printf("%s\n", argv[0]);
	return (0);
}
